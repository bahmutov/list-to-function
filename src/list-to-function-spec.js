'use strict'


/* eslint-env mocha */
const listToFunction = require('.')

describe('list-to-function', () => {
  it('write this test', () => {
    console.assert(listToFunction, 'should export something')
  })
})
